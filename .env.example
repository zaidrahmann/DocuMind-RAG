# DocuMind-RAG Environment Variables
# Copy to .env and fill in your values: cp .env.example .env

# HuggingFace Inference API key (required for HFGenerator)
# Create at: https://huggingface.co/settings/tokens (enable "Inference" or read access)
HF_API_KEY=

# Generator: ollama (default) or hf. Use ollama for local LLM (no API key).
DOCUMIND_GENERATOR=ollama

# Optional: Ollama (when DOCUMIND_GENERATOR=ollama)
# OLLAMA_URL=http://localhost:11434
# OLLAMA_MODEL=llama3

# Optional: HuggingFace (when DOCUMIND_GENERATOR=hf)
# HF_MODEL=mistralai/Mistral-7B-Instruct-v0.2

# Optional: Reranking (retrieve top-20, rerank with cross-encoder, pass top-5 to LLM). Default: true.
# DOCUMIND_USE_RERANKER=true
# DOCUMIND_RERANKER_MODEL=cross-encoder/ms-marco-MiniLM-L-6-v2
